function shuffleTwo(arr) {
    let newArr = [];
    
    for(let i = 0; i < arr.length; i++) {
        let randomNum = Math.floor(Math.random() * arr.length) + 1;
    }
}

const wordList = [
    ["agreement", "일치"],
    ["assure", "보장하다"],
    ["favorably", "호의적으로"],
    ["address", "주소, 연설하다"],
    ["capacity", "재능"],
]

shuffleTwo(wordList);